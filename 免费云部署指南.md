# 化工项目管理系统 - 免费云部署指南

> 将系统部署到云端，生成访问网址供他人使用（完全免费）

---

## 📋 部署方案概览

| 组件 | 推荐平台 | 费用 | 部署难度 |
|------|----------|------|----------|
| **前端** | Vercel | 免费 | ⭐ 简单 |
| **后端** | Render | 免费 | ⭐⭐ 中等 |
| **代码托管** | GitHub | 免费 | ⭐ 简单 |

**部署后访问方式**:
- 前端网址: `https://你的项目名.vercel.app`
- 后端API: `https://你的项目名.onrender.com`

---

## 🎯 方案一：Vercel + Render（推荐）

### 优点
- ✅ 完全免费
- ✅ 自动部署（推送代码即部署）
- ✅ 全球CDN加速
- ✅ 免费SSL证书（HTTPS）
- ✅ 不需要信用卡

### 限制
- ⚠️ 后端免费版会休眠（15分钟无访问会休眠，第一次访问需等待30秒唤醒）
- ⚠️ 每月流量和构建时间有限制（个人使用完全够用）

---

## 📝 准备工作

### 1. 注册必要账号

#### GitHub 账号（代码托管）
1. 访问 https://github.com
2. 点击"Sign up"注册
3. 验证邮箱

#### Vercel 账号（前端部署）
1. 访问 https://vercel.com
2. 点击"Sign Up"
3. **选择"Continue with GitHub"**（用GitHub账号登录）

#### Render 账号（后端部署）
1. 访问 https://render.com
2. 点击"Get Started"
3. **选择"GitHub"登录**

---

## 🚀 部署步骤

### 第一步：上传代码到 GitHub

#### 1.1 安装 Git

**Windows**:
- 下载：https://git-scm.com/download/win
- 安装后重启命令行

**检查安装**:
```bash
git --version
```

#### 1.2 初始化 Git 仓库

打开命令行，进入项目目录：

```bash
cd C:\Users\Administrator\Desktop\xiangmu
```

初始化 Git：

```bash
# 初始化仓库
git init

# 配置用户信息
git config user.name "你的名字"
git config user.email "你的邮箱@example.com"

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"
```

#### 1.3 创建 GitHub 仓库

1. 访问 https://github.com/new
2. **Repository name**: `chemical-project-management`（项目名称）
3. **Description**: `化工项目管理系统`
4. 选择 **Public**（公开）或 **Private**（私有，推荐）
5. **不要勾选** "Initialize this repository with a README"
6. 点击 **Create repository**

#### 1.4 推送代码到 GitHub

复制页面显示的命令，或执行：

```bash
# 添加远程仓库（替换 YOUR_USERNAME 为你的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/chemical-project-management.git

# 推送代码
git branch -M main
git push -u origin main
```

**如果提示需要登录**:
- 用户名：GitHub 用户名
- 密码：需要使用 Personal Access Token（不是GitHub密码）

**创建 Token**:
1. GitHub 右上角头像 → Settings
2. 左侧 Developer settings → Personal access tokens → Tokens (classic)
3. Generate new token (classic)
4. 勾选 `repo` 权限
5. 生成并复制 token（只显示一次，请保存）

---

### 第二步：部署后端到 Render

#### 2.1 创建 Render 配置文件

在项目根目录创建 `render.yaml`:

```bash
# 在 Windows PowerShell 中执行
cd C:\Users\Administrator\Desktop\xiangmu
```

**配置文件已创建** → 见下方自动创建

#### 2.2 修改 requirements.txt

确保包含所有依赖：

```bash
# 在项目根目录执行
cd server
```

**配置文件已优化** → 见下方自动创建

#### 2.3 在 Render 创建服务

1. 访问 https://dashboard.render.com
2. 点击 **New +** → **Web Service**
3. 选择 **Connect a repository**
4. 找到 `chemical-project-management` 仓库，点击 **Connect**
5. 配置服务：
   - **Name**: `chemical-backend`（服务名称）
   - **Region**: `Singapore`（新加坡，离中国最近）
   - **Branch**: `main`
   - **Root Directory**: `server`（后端目录）
   - **Runtime**: `Python 3`
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `uvicorn main:app --host 0.0.0.0 --port $PORT`
   - **Instance Type**: **Free**（免费套餐）

6. 点击 **Create Web Service**

7. 等待部署完成（约 5-10 分钟）

8. 部署成功后，复制网址（例如：`https://chemical-backend.onrender.com`）

#### 2.4 测试后端

访问：`https://chemical-backend.onrender.com/docs`

应该能看到 Swagger API 文档页面。

---

### 第三步：部署前端到 Vercel

#### 3.1 修改前端 API 地址

编辑 `client/src/config.ts`（如果不存在则创建）：

**配置文件已自动创建** → 见下方

#### 3.2 更新前端代码中的 API 调用

**已自动处理** → 见下方自动创建的配置

#### 3.3 推送更新到 GitHub

```bash
# 在项目根目录
git add .
git commit -m "Add production config"
git push origin main
```

#### 3.4 在 Vercel 部署前端

1. 访问 https://vercel.com/dashboard
2. 点击 **Add New** → **Project**
3. 找到 `chemical-project-management` 仓库
4. 点击 **Import**
5. 配置项目：
   - **Framework Preset**: `Create React App`
   - **Root Directory**: `client`（点击 Edit 修改）
   - **Build Command**: `npm run build`
   - **Output Directory**: `build`
   - **Install Command**: `npm install`

6. **Environment Variables**（环境变量）:
   - 点击 **Add** 添加：
     - **Name**: `REACT_APP_API_URL`
     - **Value**: `https://chemical-backend.onrender.com`（你的后端网址）

7. 点击 **Deploy**

8. 等待部署完成（约 3-5 分钟）

9. 部署成功后，会显示网址（例如：`https://chemical-project-management.vercel.app`）

#### 3.5 测试前端

访问：`https://chemical-project-management.vercel.app`

应该能看到登录页面。

---

### 第四步：配置 CORS（跨域）

#### 4.1 修改后端 CORS 设置

编辑 `server/main.py`，添加 CORS 中间件：

**已自动处理** → 见下方自动更新

#### 4.2 重新部署后端

```bash
# 提交更改
git add .
git commit -m "Update CORS settings"
git push origin main
```

Render 会自动检测到更新并重新部署。

---

## ✅ 部署完成

### 访问地址

| 服务 | 网址 | 说明 |
|------|------|------|
| 前端系统 | `https://你的项目名.vercel.app` | 分享这个给用户 |
| 后端 API | `https://你的后端名.onrender.com` | 仅供系统调用 |
| API 文档 | `https://你的后端名.onrender.com/docs` | 开发者文档 |

### 使用说明

1. **分享访问链接**：将前端网址发送给用户
2. **登录信息**：
   - 用户名：`admin`
   - 密码：`admin123`

### 注意事项

⚠️ **后端休眠问题**：
- Render 免费版15分钟无访问会休眠
- 第一次访问需等待 30-50 秒唤醒
- 解决方案：使用定时 ping 服务保持唤醒（见下方）

---

## 🔧 高级配置

### 1. 保持后端持续运行

**方法一：使用 UptimeRobot 定时 Ping**

1. 访问 https://uptimerobot.com（免费）
2. 注册账号
3. Add New Monitor:
   - **Monitor Type**: HTTP(s)
   - **Friendly Name**: `Chemical Backend`
   - **URL**: `https://chemical-backend.onrender.com/docs`
   - **Monitoring Interval**: 5 minutes
4. Create Monitor

这样每 5 分钟会自动访问一次后端，防止休眠。

**方法二：使用 Cron-job.org**

1. 访问 https://cron-job.org
2. 注册账号
3. 创建定时任务：
   - **URL**: `https://chemical-backend.onrender.com/api/v1/projects/`
   - **Interval**: Every 10 minutes
4. 启用任务

---

### 2. 自定义域名（可选）

#### Vercel 自定义域名

1. 购买域名（推荐：阿里云、腾讯云）
2. Vercel Dashboard → 项目 → Settings → Domains
3. 添加你的域名
4. 按提示配置 DNS 解析

#### Render 自定义域名

1. Render Dashboard → 服务 → Settings → Custom Domains
2. 添加域名并配置 DNS

---

### 3. 数据持久化（高级）

当前版本数据存储在内存中，重启后丢失。如需持久化：

**选项一：使用 Render PostgreSQL**
- Render 提供免费 PostgreSQL 数据库
- 90 天后数据会删除（免费版限制）

**选项二：使用 Supabase**
- 访问 https://supabase.com（免费）
- 提供 PostgreSQL 数据库
- 500MB 存储空间

---

## 📱 移动端访问

部署后的系统支持移动端浏览器访问：
- iOS Safari
- Android Chrome

**建议**：添加到手机主屏幕，像 App 一样使用。

---

## 🔄 如何更新系统

### 更新步骤

1. 修改本地代码
2. 提交到 GitHub:
```bash
git add .
git commit -m "更新说明"
git push origin main
```
3. Vercel 和 Render 会**自动检测并部署**（约 3-5 分钟）

### 查看部署状态

- **Vercel**: https://vercel.com/dashboard → 项目 → Deployments
- **Render**: https://dashboard.render.com → 服务 → Events

---

## ❓ 常见问题

### Q1: 后端访问很慢怎么办？

**答**: Render 免费版在东南亚（新加坡），国内访问可能较慢。

**解决方案**:
1. 使用 UptimeRobot 保持唤醒
2. 考虑升级到付费版（$7/月）
3. 或使用国内云服务（阿里云、腾讯云）

---

### Q2: Vercel 部署失败怎么办？

**答**: 查看 Deployment Logs：

1. Vercel Dashboard → 项目 → Deployments
2. 点击失败的部署
3. 查看 Build Logs
4. 根据错误信息修复

**常见错误**:
- `Module not found`: 运行 `npm install` 安装缺失依赖
- `Build failed`: 检查 TypeScript 编译错误

---

### Q3: CORS 错误怎么办？

**答**: 确保后端 `main.py` 中 CORS 设置正确：

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # 生产环境建议指定具体域名
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

---

### Q4: 如何查看后端日志？

**答**: 
1. 访问 Render Dashboard
2. 选择你的服务
3. 点击 **Logs** 标签
4. 查看实时日志

---

### Q5: 可以使用免费数据库吗？

**答**: 可以！推荐方案：

**Supabase** (PostgreSQL):
- 免费 500MB 存储
- 50,000 月活用户
- 网址: https://supabase.com

**MongoDB Atlas**:
- 免费 512MB 存储
- 网址: https://www.mongodb.com/cloud/atlas

---

### Q6: 部署需要多长时间？

**答**: 首次部署约 **20-30 分钟**（包括学习时间）

- 上传 GitHub: 5 分钟
- 部署后端: 10 分钟
- 部署前端: 5 分钟
- 配置测试: 5-10 分钟

---

### Q7: 免费额度够用吗？

**答**: 对于个人或小团队使用完全够用：

**Vercel 免费额度**:
- 100 GB 带宽/月
- 100 次构建/月
- 无限项目

**Render 免费额度**:
- 750 小时/月（单个服务足够）
- 休眠后自动唤醒
- 15 分钟无访问休眠

---

### Q8: 如何限制访问权限？

**答**: 几种方案：

1. **前端密码保护**（当前已有登录页面）
2. **Vercel 密码保护**（付费功能）
3. **后端 JWT 认证**（需要开发）
4. **IP 白名单**（Render 付费功能）

---

## 🎁 免费部署总结

### 完全免费方案对比

| 平台 | 前端 | 后端 | 数据库 | 总费用 |
|------|------|------|--------|--------|
| **推荐方案** | Vercel | Render | Supabase | **￥0** |
| 备选方案1 | Netlify | Railway | MongoDB Atlas | **￥0** |
| 备选方案2 | Cloudflare Pages | Fly.io | PlanetScale | **￥0** |

### 性能对比

| 方案 | 访问速度 | 稳定性 | 唤醒速度 |
|------|----------|--------|----------|
| Vercel + Render | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ (30s) |
| Netlify + Railway | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ (15s) |

---

## 📞 技术支持

如果部署过程中遇到问题：

1. 查看平台官方文档
2. 检查本文档 FAQ 部分
3. 查看 GitHub Issues
4. 联系技术支持

---

## 🎉 下一步

部署完成后，你可以：

1. ✅ 分享访问链接给团队成员
2. ✅ 设置定时 Ping 保持后端运行
3. ✅ 配置自定义域名（可选）
4. ✅ 集成数据库实现数据持久化（可选）
5. ✅ 添加更多功能和优化

---

**恭喜！你已成功将系统部署到云端！** 🚀

现在任何人都可以通过网址访问你的项目管理系统了！

