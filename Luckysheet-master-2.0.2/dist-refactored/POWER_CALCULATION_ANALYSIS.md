# 风机功率计算公式验证与问题分析

## ✅ 功率计算公式是正确的

### 标准公式（国际通用）

**轴功率（kW）**：
```
P_shaft = (Q × P_t) / (3600 × η_f × 1000)
```

**电机功率（kW）**：
```
P_motor = (P_shaft / η_m) × K
```

**符号说明**：
- `Q`：风量 (m³/h)
- `P_t`：全压 (Pa)
- `η_f`：风机效率（0.65~0.85）
- `η_m`：传动效率（直联0.98，皮带0.92~0.95）
- `K`：安全系数（1.05~1.3，常温1.15，高温1.25）

### 单位推导验证

```
功率 = 流量 × 压力 / 效率

步骤1：流量单位转换
  Q(m³/h) / 3600 = Q(m³/s)

步骤2：功率计算（W）
  P(W) = Q(m³/s) × P_t(Pa) / η
       = [Q(m³/h)/3600] × P_t(Pa) / η
       = Q × P_t / (3600 × η)  [单位：W]

步骤3：转换为kW
  P(kW) = P(W) / 1000
        = Q × P_t / (3600 × η × 1000)  ✅
```

### 验证示例

**示例1：标准案例**
```
Q = 50,000 m³/h
P_t = 3,000 Pa
η_f = 0.82
η_m = 0.98
K = 1.15

P_shaft = (50000 × 3000) / (3600 × 0.82 × 1000)
        = 50.81 kW  ✅

P_motor = (50.81 / 0.98) × 1.15
        = 59.6 kW  ✅
```

**示例2：Excel截图数据（第3行）**
```
Q = 189,955 m³/h
P_t = 6,321 Pa
η = 0.81
T = 60℃

P_shaft = (189955 × 6321) / (3600 × 0.81 × 1000)
        = 411.76 kW  ✅ (Excel: 411.77 kW)

P_motor = (411.76 / 0.98) × 1.15
        = 483.19 kW  ✅ (Excel: 483.20 kW)
```

## ⚠️ 实际问题：功率数值"太大"的真正原因

### 问题根源

当前代码使用 `calculatedFlow` 和 `calculatedPressure` 计算功率：

```javascript
const calculatedFlow = φ × (π/4) × D² × u × 3600 × inletFactor;
const calculatedPressure = ψ × ρ × u²;
const shaftPower = (calculatedFlow × calculatedPressure) / (3600 × η × 1000);
```

**问题**：当 `D`（叶轮直径）不匹配时，`calculatedFlow` 和 `calculatedPressure` 会远大于用户输入的目标值！

### 案例分析

**用户输入**：
```
Q_target = 16,000 m³/h
P_target = 4,500 Pa
D = 2.2 m（给定）
n = 960 r/min
```

**系统计算**（假设推荐到某型号，φ=0.15, ψ=0.45）：
```
u = π × 2.2 × 960 / 60 = 110.58 m/s
Q_calc = 0.15 × (π/4) × 2.2² × 110.58 × 3600 = 226,998 m³/h  (是目标的14倍！)
P_calc = 0.45 × 1.056 × 110.58² = 5,811 Pa
```

**结果功率**：
```
P_shaft = (226998 × 5811) / (3600 × 0.8 × 1000) = 458 kW
P_motor = 458 / 0.98 × 1.15 = 537 kW  ⚠️ 太大了！
```

**理论功率**（基于用户输入）：
```
P_shaft = (16000 × 4500) / (3600 × 0.8 × 1000) = 25 kW
P_motor = 25 / 0.98 × 1.15 = 29.3 kW  ✅ 合理
```

## 🎯 解决方案

### 方案A：显示两种功率（推荐）

```javascript
// 1. 基于用户输入的理论功率（需求功率）
const theoreticalShaftPower = (inputs.flowRate × inputs.totalPressure) / (3600 × fanEfficiency × 1000);
const theoreticalMotorPower = theoreticalShaftPower / transmissionEff × safetyFactor;

// 2. 基于计算值的实际功率（当前选型的功率）
const actualShaftPower = (calculatedFlow × calculatedPressure) / (3600 × fanEfficiency × 1000);
const actualMotorPower = actualShaftPower / transmissionEff × safetyFactor;

// 界面同时显示两种功率
document.getElementById('theoreticalPower').textContent = theoreticalMotorPower.toFixed(2) + ' kW（需求）';
document.getElementById('actualPower').textContent = actualMotorPower.toFixed(2) + ' kW（选型）';
```

### 方案B：优化叶轮直径D（根本解决）

Excel会通过迭代找到合适的D，使得 `calculatedFlow ≈ targetFlow` 和 `calculatedPressure ≈ targetPressure`。

```javascript
// 粗算叶轮直径（Excel C10公式）
const roughD = 27 / n × Math.sqrt(P_target / (2 × ρ × ψ));

// 使用roughD替代固定的D进行计算
const actualImpellerD = roughD; // 而不是 inputs.impellerDiameter
```

## 📊 当前实现状态

已在代码中实现方案A：
- 主界面显示"理论功率"（基于用户输入）
- 7行对比表显示"实际功率"（基于计算值）
- 两种功率都有明确标注

## 🔧 推荐改进

1. **默认显示理论功率**：更符合用户预期
2. **添加功率对比说明**：解释为何两者有差异
3. **优化D值计算**：使用粗算公式自动调整D
4. **添加警告提示**：当实际功率>理论功率×1.5时提醒用户

## 📝 参考标准

- GB/T 1236：通风机空气动力性能试验方法
- ISO 5801：Industrial fans - Performance testing using standardized airways
- AMCA 210：Laboratory Methods of Testing Fans for Aerodynamic Performance Rating

---

**结论**：功率计算公式完全正确，"太大"的问题是因为叶轮直径D固定导致计算值偏离目标值。建议采用方案A（同时显示两种功率）并优化D值计算。


