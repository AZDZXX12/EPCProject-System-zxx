# 电缆选型工具 - 纯前端版本

> 无需后端服务器，所有计算都在浏览器中完成！

## ✨ 特性

- 🚀 **零后端依赖** - 无需数据库和API服务器
- ⚡ **即开即用** - 只需Node.js即可运行
- 📦 **易于部署** - 可部署到任何静态文件托管服务
- 🔒 **数据安全** - 所有计算在本地完成，无数据上传
- 💪 **完整功能** - 包含所有电缆计算、3D展示功能

## 🎯 快速开始

### 方式一：直接运行（推荐）

#### Windows
双击 `start-frontend-only.bat` 文件

#### Mac/Linux
```bash
chmod +x start-frontend-only.sh
./start-frontend-only.sh
```

### 方式二：手动启动

```bash
# 进入前端目录
cd frontend

# 安装依赖（首次运行）
npm install

# 启动开发服务器
npm run dev
```

访问: http://localhost:3000

## 📦 生产部署

### 构建静态文件

```bash
cd frontend
npm run build
```

构建产物在 `frontend/dist` 目录，可以部署到：

### 部署选项

#### 1. 使用Nginx

```bash
# 复制构建产物
cp -r frontend/dist/* /var/www/html/

# 使用提供的nginx配置
cp frontend/nginx.conf /etc/nginx/conf.d/cable-selector.conf
```

#### 2. 使用Docker（纯前端）

```bash
# 使用Docker Compose
docker-compose -f frontend-only-docker-compose.yml up -d

# 或直接构建生产镜像
cd frontend
docker build -f Dockerfile.production -t cable-selector .
docker run -p 80:80 cable-selector
```

#### 3. 部署到Vercel

```bash
cd frontend
npm install -g vercel
vercel
```

#### 4. 部署到Netlify

```bash
cd frontend
npm run build
# 上传 dist 目录到 Netlify
```

#### 5. 部署到GitHub Pages

```bash
cd frontend
npm run build
# 将 dist 目录内容推送到 gh-pages 分支
```

## 🏗️ 项目结构

```
frontend/
├── src/
│   ├── components/        # React组件
│   ├── data/             # 电缆数据（本地）
│   │   └── cableSpecs.ts # 15种电缆规格
│   ├── utils/            # 计算引擎
│   │   ├── cableCalculator.ts    # 主计算逻辑
│   │   └── currentCorrection.ts  # 校正系数
│   ├── types/            # 类型定义
│   └── App.tsx           # 主应用
├── package.json
└── vite.config.ts
```

## 💡 技术实现

### 本地计算引擎

所有计算都在浏览器中完成：

1. **电缆数据** - 存储在 `src/data/cableSpecs.ts`
2. **计算逻辑** - 实现在 `src/utils/cableCalculator.ts`
3. **校正系数** - 基于IEC标准的温度和敷设校正

### 支持的功能

✅ 载流量计算（IEC 60287-1-1）  
✅ 温度校正（IEC 60512）  
✅ 敷设方式校正（IEC 60364-5-52）  
✅ 电压降计算  
✅ 3D电缆模型展示  
✅ 多方案对比  
✅ 成本估算  

## 📊 内置电缆规格

包含15种常用电缆规格：

- **YJV系列** (XLPE绝缘): 16mm² ~ 300mm²
- **VV系列** (PVC绝缘): 25mm², 35mm²
- **YJV22系列** (铠装): 50mm², 70mm²

### 添加新规格

编辑 `frontend/src/data/cableSpecs.ts`:

```typescript
export const cableSpecs: CableSpecData[] = [
  // 现有规格...
  {
    id: 16,
    type: "YJV-0.6/1kV",
    core_count: 4,
    cross_section: 400.0,
    current_rating: 700.0,
    rated_voltage: 380,
    max_temp: 90,
    insulation_material: "XLPE",
    shield_type: "copper",
    price_per_meter: 450.0,
    voltage_drop_coef: 0.052
  }
];
```

## 🧪 测试

```bash
cd frontend
npm test
```

## 📈 性能优化

前端版本已优化：

- ⚡ 使用Vite构建，启动速度快
- 🎨 Material-UI组件按需加载
- 🔄 计算结果实时生成
- 💾 浏览器本地缓存

## 🆚 前端版 vs 全栈版

| 特性 | 纯前端版 | 全栈版 |
|------|----------|--------|
| 部署难度 | ⭐ 简单 | ⭐⭐⭐ 复杂 |
| 运行环境 | 浏览器 | 浏览器+服务器 |
| 数据存储 | 本地 | PostgreSQL |
| 可扩展性 | 中等 | 高 |
| 管理后台 | ❌ | ✅ |
| 用户系统 | ❌ | 可扩展 |
| API接口 | ❌ | ✅ |

## 🔧 常见问题

### Q: 如何修改电缆价格？
**A:** 编辑 `frontend/src/data/cableSpecs.ts`，修改 `price_per_meter` 字段

### Q: 计算结果准确吗？
**A:** 计算基于IEC和GB标准，与后端版本算法完全一致

### Q: 能否离线使用？
**A:** 可以！构建后的静态文件可完全离线运行

### Q: 支持哪些浏览器？
**A:** Chrome/Edge/Firefox/Safari 最新版本

## 📝 更新日志

### v2.0.0 (纯前端版本)
- ✨ 移除后端依赖
- 🚀 本地化所有计算逻辑
- 📦 简化部署流程
- ⚡ 提升加载速度

## 📄 许可证

MIT License

## 🤝 贡献

欢迎提交Issue和Pull Request！

---

**享受零配置的电缆选型体验！** 🎉

