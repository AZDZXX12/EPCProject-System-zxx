# 🎨 简化版Blender编辑器使用指南

**版本**：v1.0  
**更新时间**：2025-12-01

---

## ✅ 已完成功能

### 核心编辑功能 ⭐⭐⭐⭐⭐

#### 1. **添加对象**
- ✅ 立方体 (Cube)
- ✅ 球体 (Sphere)
- ✅ 圆柱 (Cylinder)

#### 2. **变换工具**
- ✅ 移动 (Translate) - G键
- ✅ 旋转 (Rotate) - R键
- ✅ 缩放 (Scale) - S键

#### 3. **对象操作**
- ✅ 选择对象
- ✅ 复制对象 (Shift+D)
- ✅ 删除对象 (X键)
- ✅ 显示/隐藏
- ✅ 锁定/解锁

#### 4. **视图控制**
- ✅ 轨道旋转（鼠标拖拽）
- ✅ 缩放（鼠标滚轮）
- ✅ 平移（右键拖拽）
- ✅ 网格显示
- ✅ 坐标轴辅助器

#### 5. **场景管理**
- ✅ 对象列表（Outliner）
- ✅ 对象层级显示
- ✅ 实时状态栏

---

## 🎯 界面布局

```
┌─────────────────────────────────────────────────────────┐
│  [+立方体] [+球体] [+圆柱] | [移动] [旋转] [缩放] | ...  │  ← 顶部工具栏
├─────────────────────────────────────────┬───────────────┤
│                                         │  场景对象     │
│                                         │  ┌──────────┐ │
│          3D视图区域                      │  │ Cube_1  │ │
│                                         │  │ Sphere_2│ │
│          (可拖拽旋转/缩放)                │  │ Cylinder│ │
│                                         │  └──────────┘ │
│                                         │               │
│                                         │  [眼睛][锁]   │
├─────────────────────────────────────────┴───────────────┤
│  对象:3 | 选中:Cube_1 | 模式:移动 | 💡 快捷键提示        │  ← 底部状态栏
└─────────────────────────────────────────────────────────┘
```

---

## 🚀 快速开始

### 访问编辑器

方式1：直接访问（推荐）
```
http://localhost:3001/blender-editor
```

方式2：从数字孪生页面跳转
```
数字孪生页面 → Editor模式 → "打开Blender编辑器"按钮
```

### 基础操作流程

#### 1. 添加对象
```
点击顶部工具栏 → [+立方体] 
→ 场景中出现立方体
→ 自动选中
```

#### 2. 移动对象
```
选中对象 → 点击[移动]按钮
→ 出现移动控制器（3个箭头）
→ 拖拽箭头移动对象
```

#### 3. 旋转对象
```
选中对象 → 点击[旋转]按钮
→ 出现旋转控制器（3个圆环）
→ 拖拽圆环旋转对象
```

#### 4. 缩放对象
```
选中对象 → 点击[缩放]按钮
→ 出现缩放控制器（3个方块）
→ 拖拽方块缩放对象
```

---

## ⌨️ 快捷键

### 基础操作
| 快捷键 | 功能 | 说明 |
|--------|------|------|
| G | 移动模式 | Grab（抓取） |
| R | 旋转模式 | Rotate |
| S | 缩放模式 | Scale |
| X | 删除对象 | Delete |
| Shift+D | 复制对象 | Duplicate |

### 视图控制
| 操作 | 功能 |
|------|------|
| 鼠标左键拖拽 | 旋转视图 |
| 鼠标滚轮 | 缩放视图 |
| 鼠标右键拖拽 | 平移视图 |
| 鼠标中键拖拽 | 平移视图 |

### 视图角度
| 按钮 | 功能 | Blender快捷键 |
|------|------|---------------|
| 前 | 前视图 | Numpad 1 |
| 右 | 右视图 | Numpad 3 |
| 顶 | 顶视图 | Numpad 7 |

---

## 🎨 功能详解

### 1. 添加对象

#### 立方体
```
用途：基础建模、容器、建筑
特点：6个面，8个顶点
默认大小：1x1x1
```

#### 球体
```
用途：圆形物体、装饰
特点：平滑表面，32段精度
默认半径：0.5
```

#### 圆柱
```
用途：管道、柱子、轴
特点：圆形截面，32段精度
默认尺寸：半径0.5，高度1
```

### 2. 变换工具

#### 移动 (Translate)
```
功能：改变对象位置
控制器：3个彩色箭头
- 红色箭头 = X轴
- 绿色箭头 = Y轴
- 蓝色箭头 = Z轴
```

#### 旋转 (Rotate)
```
功能：改变对象方向
控制器：3个彩色圆环
- 红色圆环 = 绕X轴旋转
- 绿色圆环 = 绕Y轴旋转
- 蓝色圆环 = 绕Z轴旋转
```

#### 缩放 (Scale)
```
功能：改变对象大小
控制器：3个彩色方块
- 红色方块 = X轴缩放
- 绿色方块 = Y轴缩放
- 蓝色方块 = Z轴缩放
```

### 3. 对象管理

#### 对象列表（右侧面板）
```
显示内容：
- 对象图标
- 对象名称
- 可见性开关（眼睛图标）
- 锁定开关（锁图标）

操作：
- 点击选中对象
- 点击眼睛隐藏/显示
- 点击锁定防止编辑
```

#### 对象属性
```
每个对象包含：
- ID：唯一标识符
- 名称：可读名称
- 类型：box/sphere/cylinder
- 可见性：visible/hidden
- 锁定状态：locked/unlocked
```

---

## 💡 使用技巧

### 技巧1：精确建模
```
1. 使用网格对齐
2. 观察坐标轴颜色
3. 单轴移动更精确
```

### 技巧2：快速复制
```
1. 选中对象
2. 点击复制按钮
3. 新对象自动偏移
4. 继续编辑
```

### 技巧3：组织场景
```
1. 使用描述性名称
2. 隐藏不需要的对象
3. 锁定完成的对象
```

### 技巧4：视图导航
```
1. 使用视图角度快捷按钮
2. 双击对象聚焦
3. 滚轮缩放查看细节
```

---

## 🔧 与Blender对比

### 保留的核心功能 ✅
- ✅ 添加基础几何体
- ✅ 移动/旋转/缩放
- ✅ 对象选择和管理
- ✅ 视图控制
- ✅ 对象列表（Outliner）
- ✅ 快捷键操作

### 简化的功能 ⚡
- ⚡ 只保留3种基础几何体
- ⚡ 简化的材质系统
- ⚡ 基础的变换工具
- ⚡ 简洁的UI布局

### 未包含的高级功能 ❌
- ❌ 编辑模式（顶点/边/面）
- ❌ 修改器系统
- ❌ 动画系统
- ❌ 渲染引擎
- ❌ 节点编辑器
- ❌ UV展开
- ❌ 雕刻工具

---

## 🎯 适用场景

### ✅ 适合
- 快速创建简单3D场景
- 工业设备布局
- 基础模型摆放
- 原型设计
- 教学演示

### ❌ 不适合
- 复杂建模
- 角色动画
- 高精度雕刻
- 专业渲染
- 游戏资产制作

---

## 🚀 扩展建议

### 如需更多功能，可以：

#### 方案A：使用完整Blender
```
优势：
- 专业级功能
- 强大的建模工具
- 完整的材质系统

使用方式：
cd blender
启动Blender.bat
```

#### 方案B：扩展当前编辑器
```
可添加功能：
- 更多几何体类型
- 材质编辑器
- 灯光系统
- 导入/导出GLB
- 撤销/重做
```

---

## 📊 性能优化

### 对象数量建议
- ✅ 1-50个对象：流畅
- ⚠️ 50-100个对象：可接受
- ❌ 100+个对象：考虑优化

### 优化技巧
1. 减少对象数量
2. 使用实例化（相同对象）
3. 降低几何体精度
4. 隐藏不需要的对象

---

## 🐛 常见问题

### Q: 对象无法选中？
A: 检查对象是否被锁定（锁图标）

### Q: 变换控制器不显示？
A: 确保对象已选中且未锁定

### Q: 视图旋转不流畅？
A: 减少场景中的对象数量

### Q: 如何重置视图？
A: 刷新页面或使用视图角度按钮

---

## 📝 开发信息

### 技术栈
- React 18 + TypeScript
- React Three Fiber
- @react-three/drei
- Three.js
- Lucide React Icons

### 文件结构
```
client/src/components/DigitalTwin3D/
├── BlenderLikeEditor.tsx       # 主编辑器组件
├── BlenderLikeEditor.css       # 编辑器样式
└── ...

client/src/pages/
└── BlenderEditorPage.tsx       # 编辑器页面
```

---

## 🎉 总结

### 核心优势
- ✅ **简单易用** - 保留核心功能
- ✅ **Web原生** - 无需安装
- ✅ **实时预览** - 即时反馈
- ✅ **轻量级** - 快速加载

### 使用建议
1. 用于快速原型设计
2. 结合完整Blender使用
3. 导出模型到数字孪生系统

---

**开始使用简化版Blender编辑器吧！** 🎨✨

访问地址：`http://localhost:3001/blender-editor`
