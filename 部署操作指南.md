# 🚀 EPC系统部署操作指南

## 📋 部署信息
- **GitHub仓库**: https://github.com/AZDZXX12/EPCProject-System
- **Render服务名**: EPCProject-System
- **部署Hook**: https://api.render.com/deploy/srv-d42uq6i4d50c73a52uh0?key=1BWfJf71pvg

---

## ✅ 完整操作步骤

### 第一步：初始化Git仓库并提交代码

在项目根目录 `C:\Users\Administrator\Desktop\xiangmu` 打开PowerShell，执行以下命令：

```powershell
# 1. 切换到项目根目录
cd C:\Users\Administrator\Desktop\xiangmu

# 2. 初始化Git仓库
git init

# 3. 添加远程仓库
git remote add origin https://github.com/AZDZXX12/EPCProject-System.git

# 4. 添加所有文件到暂存区
git add .

# 5. 提交代码
git commit -m "feat: 完整EPC项目管理系统 - 总包施工管理全面优化版"

# 6. 推送到GitHub主分支
git push -u origin main
```

**如果推送失败（分支名称问题），执行：**
```powershell
git branch -M main
git push -u origin main
```

---

### 第二步：创建Render部署配置文件

已为您创建以下配置文件：
- ✅ `render.yaml` - Render部署配置
- ✅ `.gitignore` - Git忽略文件
- ✅ `client/package.json` - 前端配置（已有）
- ✅ `server/requirements.txt` - 后端依赖（已有）

---

### 第三步：在Render上部署

#### 方式1：通过Render Dashboard（推荐）

1. **登录Render**
   - 访问：https://dashboard.render.com/
   - 使用GitHub账号登录

2. **创建后端服务（Python）**
   - 点击 "New +" → "Web Service"
   - 连接GitHub仓库：`AZDZXX12/EPCProject-System`
   - 配置如下：
     ```
     Name: epc-backend
     Environment: Python 3
     Build Command: pip install -r server/requirements.txt
     Start Command: cd server && python quick-start-sqlite.py
     Plan: Free
     ```
   - 环境变量：
     ```
     PYTHON_VERSION=3.11
     PORT=8000
     ```

3. **创建前端服务（Node.js）**
   - 点击 "New +" → "Static Site"
   - 连接同一个GitHub仓库
   - 配置如下：
     ```
     Name: epc-frontend
     Build Command: cd client && npm install && npm run build
     Publish Directory: client/build
     ```
   - 环境变量：
     ```
     REACT_APP_API_URL=https://epc-backend.onrender.com
     NODE_VERSION=18
     ```

4. **部署**
   - 点击 "Create Static Site" 和 "Create Web Service"
   - Render会自动开始构建和部署

#### 方式2：使用Deploy Hook（快速部署）

如果服务已创建，直接触发部署：

```powershell
# 使用curl触发部署
curl -X POST "https://api.render.com/deploy/srv-d42uq6i4d50c73a52uh0?key=1BWfJf71pvg"
```

或在浏览器中直接访问：
```
https://api.render.com/deploy/srv-d42uq6i4d50c73a52uh0?key=1BWfJf71pvg
```

---

### 第四步：验证部署

1. **检查后端服务**
   - 访问：`https://epc-backend.onrender.com/docs`
   - 应该看到FastAPI的API文档

2. **检查前端服务**
   - 访问：`https://epc-frontend.onrender.com`
   - 应该看到EPC系统登录页面

3. **测试功能**
   - 登录系统
   - 测试总包施工管理功能
   - 测试甘特图功能
   - 测试设备管理功能

---

## 🔧 常见问题解决

### 问题1：Git推送失败（需要认证）

**解决方案**：
```powershell
# 使用个人访问令牌（Personal Access Token）
# 1. 在GitHub生成Token: Settings → Developer settings → Personal access tokens
# 2. 使用Token推送
git remote set-url origin https://YOUR_TOKEN@github.com/AZDZXX12/EPCProject-System.git
git push -u origin main
```

### 问题2：Render构建失败

**检查清单**：
- ✅ `requirements.txt` 文件存在且格式正确
- ✅ `package.json` 文件存在且格式正确
- ✅ 构建命令路径正确（cd server / cd client）
- ✅ Python版本设置为3.11
- ✅ Node版本设置为18

### 问题3：前端无法连接后端

**解决方案**：
1. 确认后端服务URL正确
2. 更新前端环境变量：
   ```
   REACT_APP_API_URL=https://你的后端服务.onrender.com
   ```
3. 重新构建前端

### 问题4：数据库文件丢失

**解决方案**：
Render的免费版不支持持久化存储，需要：
1. 使用Render的PostgreSQL服务（推荐）
2. 或使用外部数据库服务

---

## 📝 快速命令清单

### Git操作
```powershell
# 查看状态
git status

# 添加文件
git add .

# 提交
git commit -m "your message"

# 推送
git push origin main

# 查看远程仓库
git remote -v
```

### 本地测试
```powershell
# 启动后端
cd server
python quick-start-sqlite.py

# 启动前端（新终端）
cd client
npm start
```

---

## 🎯 部署检查清单

在推送代码前，确认以下文件存在：

- ✅ `.gitignore` - Git忽略文件
- ✅ `render.yaml` - Render配置（可选）
- ✅ `README.md` - 项目说明
- ✅ `client/package.json` - 前端依赖
- ✅ `server/requirements.txt` - 后端依赖
- ✅ `server/quick-start-sqlite.py` - 后端启动文件

---

## 📞 需要帮助？

如果遇到问题：
1. 查看Render的构建日志
2. 检查环境变量配置
3. 确认GitHub仓库权限
4. 查看本文档的"常见问题解决"部分

---

**部署完成后，您的EPC系统将在以下地址访问：**
- 🌐 前端：https://epc-frontend.onrender.com
- 🔧 后端：https://epc-backend.onrender.com
- 📚 API文档：https://epc-backend.onrender.com/docs

🎉 祝部署顺利！

